_format_version: "3.0"
_transform: true

services:
  - name: atlasstack-api
    url: http://api:8000
    routes:
      - name: atlasstack-api-route
        paths:
          - /api
          - /docs
          - /health
