apiVersion: v1
kind: Namespace
metadata:
  name: atlasstack
  labels:
    name: atlasstack
    app.kubernetes.io/name: atlasstack
    app.kubernetes.io/component: namespace
---
apiVersion: v1
kind: ResourceQuota
metadata:
  name: atlasstack-quota
  namespace: atlasstack
spec:
  hard:
    requests.cpu: "20"
    requests.memory: 64Gi
    limits.cpu: "40"
    limits.memory: 128Gi
    persistentvolumeclaims: "20"
    services: "20"
---
apiVersion: v1
kind: LimitRange
metadata:
  name: atlasstack-limits
  namespace: atlasstack
spec:
  limits:
    - default:
        cpu: "1"
        memory: 2Gi
      defaultRequest:
        cpu: 100m
        memory: 256Mi
      max:
        cpu: "8"
        memory: 32Gi
      min:
        cpu: 50m
        memory: 128Mi
      type: Container
