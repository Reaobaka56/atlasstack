apiVersion: v1
kind: ConfigMap
metadata:
  name: jaeger-config
  namespace: atlasstack
data:
  jaeger.yml: |
    jaeger:
      serviceName: atlasstack
      sampler:
        type: const
        param: 1
      reporter:
        logSpans: true
        localAgentHostPort: jaeger-agent:6831
      headers:
        jaegerDebugHeader: jaeger-debug-id
        jaegerBaggageHeader: jaeger-baggage
        traceBaggageHeaderPrefix: uberctx-
